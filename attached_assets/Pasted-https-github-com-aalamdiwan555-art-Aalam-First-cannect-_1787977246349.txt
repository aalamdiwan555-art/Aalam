https://github.com/aalamdiwan555-art/Aalam

First cannect GitHub and fix that all if there are already fixed fix next 




CRITICAL — bitmap ownership/leak on capture callback exceptions. aalam-repo/app/src/main/java/com/autopilot/driver/capture/ScreenCaptureController.kt:38-52: raw/cropped frame are only released after onFrame returns. If onFrame throws (or any downstream callback failure occurs), execution skips line 51 and both bitmaps leak; for cropped frames the crop is also not released. Fix with try/finally around onFrame, recycling frame and raw exactly once, or define transfer ownership explicitly and catch callback exceptions. Add test with throwing callback and repeated frames, plus bitmap allocation/recycling instrumentation.

CRITICAL — processing cancellation can leave UI/runtime state and allows stale work to publish. .../service/AutopilotService.kt:148-205,208-220: pause/stop cancels processingJob, but a job already inside frameMutex may finish/call snapshot = ... and publishSnapshot() after pause/stop; FrameProcessor receives a snapshot captured before the state transition. isPaused is checked only for qualification, not before publishing. Fix use a generation/session token, check currentCoroutineContext().ensureActive() and state/token immediately before every state mutation/publish, and join/cancel-and-wait during stop/pause. Add race tests for frame completion concurrent with pause/stop.

CRITICAL — duplicate ACTION_START starts a second capture without stopping the first. .../AutopilotService.kt:100-145: each start constructs a new controller/projection and overwrites captureController; prior VirtualDisplay/ImageReader/projection remain active and continue callbacks, while only the newest is stopped. Fix reject start while running or stop/await old session before replacing it; serialize lifecycle commands. Add repeated-start and start-vs-stop tests.

HIGH — screen coordinate assumptions are undocumented and fragile. .../capture/ScreenCaptureController.kt:32-37,54-62 and .../service/AutopilotService.kt:148-183: capture dimensions use deprecated defaultDisplay.getRealMetrics; OCR bounds are assumed identical to Accessibility dispatchGesture coordinates. Rotation changes, cutouts, display insets, Android multi-window/secondary displays, and API display metrics can make screenshot pixels differ from gesture screen coordinates. Fix obtain the actual display/window geometry, track rotation/insets/display ID, transform OCR bounds through an explicit capture→gesture matrix, and validate dimensions at action time. Add portrait/landscape, cutout/inset, density, and rotation tests.

HIGH — OCR resource lifecycle/API misuse. .../ocr/TextDetector.kt:15-21,35-37: Tasks.await blocks an IO thread and is not cancellation-aware; recognizer.close() can race an in-flight process, and close failures/Task failures propagate into frame handling. Use ML Kit Task.await coroutine integration with cancellation, keep a service-owned lifecycle, await/cancel in-flight recognition before close, and convert failures to diagnostics. Add cancellation/close-during-detect tests.

HIGH — hard-coded OCR confidence is misleading and drives false actions. .../ocr/TextDetector.kt:22-30: every line gets confidence 0.85f, regardless of ML Kit confidence (which may be unavailable but must then be treated as unknown, not high). Consequently TargetDetector and service thresholds always pass for any nonempty OCR line, and price confidence is also fabricated. Fix use actual block/line confidence where available or conservative calibrated confidence; require multiple signals/geometry. Add low-quality/blurred OCR and confidence-threshold tests.

HIGH — standalone price fallback is a major false-positive path. .../matcher/PriceParser.kt:34-45: if no currency marker exists, any number in any OCR line (order number, item count, address, time, year) becomes a price; the highest confidence tie selects the first, and confidence only drops from the fabricated 0.85 to 0.65. Service accepts prices at >=.72, so currency-less numbers normally do not qualify today only because of the arbitrary constant, but changing confidence or OCR values can enable dangerous taps. Fix require currency/context or a configured price-region/label, reject implausible IDs/counts, preserve occurrence context, and define tie-breaking. Add tests for Order 125 item 7, dates/times, IDs, multiple standalone numbers, and no-currency screens.

HIGH — parser timestamp inconsistency. .../matcher/PriceParser.kt:14-16,25-31,38-45: currency detections preserve block.timestampMs, but standalone detections use nowMs; a stale OCR block can appear fresh. Fix always propagate source timestamp and enforce frame-age checks before matching/action. Add stale-text tests.

HIGH — matcher regex accepts malformed/ambiguous amounts and misses common formats. .../matcher/PriceParser.kt:10-12: comma/space grouping permits mixed separators and groups of 2–3 digits without validating locale; decimals are limited to 1–2 digits; currency symbols such as $ are intentionally absent but OCR variants (Rs:/INR:) and Unicode spacing may fail. replace(",", "") treats malformed 12,34,567 as valid by accident (possibly intended Indian grouping, but not specified). Fix locale-aware amount grammar, normalize Unicode whitespace/OCR confusions explicitly, reject malformed separators, and define decimal policy. Add malformed/Indian grouping/Unicode/OCR-variant tests.

HIGH — target fuzzy matcher creates cross-word false positives. .../detector/TargetDetector.kt:69-86: removes all spaces, then searches every fixed-length window, so arbitrary text containing a near-match across word boundaries can be treated as a button. It also uses block.text instead of already-normalized text and accepts substring matches with no word/bounding-box/UI context. Fix token/word-boundary matching, restrict fuzzy matching to short expected labels and whole line, require stronger score plus spatial/button-region checks, and use normalized text. Add adversarial tests (unrelated ... accep..., embedded words, punctuation, multilingual spacing).

HIGH — target bounds are not verified as originating from the current screen/frame. .../detector/TargetDetector.kt:29-61: only clamps OCR Rect to supplied dimensions; negative/out-of-frame or empty bounds silently become a detected=false result, while partially out-of-frame boxes can produce a tap at a misleading clamped location. Fix reject excessively clipped boxes, validate nonzero dimensions and frame timestamp, and transform coordinates explicitly. Add out-of-bounds/clipping tests.

HIGH — target and price can come from different OCR lines/regions. .../service/AutopilotService.kt:164-183 plus PriceParser.kt:14-35: any screen-wide price and any screen-wide “Accept” are combined, with no spatial association or screen-state correlation. This can tap Accept for an unrelated price/card. Fix associate detections by container/nearby regions, require both in same frame and expected layout, and optionally require temporal stability of both independently. Add multi-card/mismatched-region tests.

HIGH — click deduplication key is unstable and insufficient. .../service/AutopilotService.kt:171-183 and .../automation/ClickController.kt:23-27: a 1-pixel OCR-box shift resets candidate stability, while a static stale box/price allows repeated taps every 900 ms indefinitely; no post-click screen-state verification exists. Fix quantize/track target identity, impose action cooldown per screen state, verify target disappearance/state change after gesture, and cap retries. Add jitter and unchanged-screen repeated-action tests.

MEDIUM — gesture cancellation guard race. .../automation/AutopilotAccessibilityService.kt:30-63: cancelPendingGestures() clears gestureInFlight without actually cancelling Android’s gesture. A new gesture can be dispatched while the old callback is still pending; old callback then clears the flag and can interfere with the new gesture’s guard. ClickController cancellation also resumes nothing by design, leaving its caller waiting until callback (possibly forever if service dies). Fix associate a monotonically increasing gesture ID/callback, use actual GestureDescription cancellation where supported/appropriate, and guarantee continuation completion on service destruction. Add overlapping/cancel/service-destroy tests.

MEDIUM — accessibility service configuration/API behavior is under-validated. .../automation/AutopilotAccessibilityService.kt:21-24,66-69 sets singleton only on connect and clears on destroy, but no readiness/connected atomic state is checked before dispatch; service can disappear between lookup and tap. Fix lifecycle-safe binder/state handling and return deterministic failure. Add connect/disconnect race tests.

MEDIUM — diagnostics falsely report healthy OCR/OpenCV. .../service/FrameProcessor.kt:32-40,56-58 always sets OCR READY after success and OpenCV based only on initialization; .../ui/AutopilotViewModel.kt:98-105 hard-codes OCR/OpenCV READY for every broadcast, even capture errors and stale updates. Fix carry detector/preprocessor status and failures through FrameResult/broadcast. Add error-state and stale-broadcast tests.

MEDIUM — source bitmap can be recycled while producer/consumer ownership is unclear. .../service/FrameProcessor.kt:31-40 always recycles source; this is currently likely intended because capture creates it, but the public processor API accepts arbitrary caller-owned bitmaps. Any future caller/use-after-return will crash. Document ownership or make processor non-owning and recycle in capture callback finally. Add ownership contract tests.

MEDIUM — OpenCV preprocessing costs a full-size duplicate per frame and has no backpressure beyond one processing job. .../opencv/ImagePreprocessor.kt:16-30, capture controller line 37: full-screen Mat/Bitmap allocations at capture cadence can cause GC/OOM; dropped frames are recycled, but capture still allocates each frame. Add bounded/latest-frame channel or ImageReader throttling, downsample/crop to relevant ROI, and allocation/performance tests.

MEDIUM — OpenCV initialization is eager and potentially repeated per service instance. .../opencv/ImagePreprocessor.kt:9-14: initLocal() runs in constructor on the service thread; initialization failure is swallowed and only reflected after processing. Fix process-wide initialization off main thread with explicit diagnostic/error handling. Add initialization failure tests.

MEDIUM — notification/service API edge cases. .../service/AutopilotService.kt:110-145,300-310: projection/session setup failures leave foreground/error handling dependent on platform behavior; API 34+ foreground-service media-projection prerequisites and notification permission are not explicitly handled. Verify manifest/runtime prerequisites, call startForeground at the required timing, and test denied notification/projection, API 29/33/34+.

Missing regression coverage: no FrameProcessor integration tests; no ScreenCaptureController lifecycle/bitmap tests; no AutopilotService lifecycle/concurrency tests; no Accessibility gesture tests; no OCR failure/cancellation/confidence tests; no coordinate transform/rotation/insets tests; no multilingual fuzzy-target adversarial tests; no parser malformed/standalone/staleness tests; no cross-region price-target association tests; no cooldown/post-click verification tests; no OpenCV allocation/failure tests; no API-level foreground-service/projection tests.

Prioritized audit (workspace-relative paths; line ranges exact):

BLOCKER build reproducibility: aalam-repo has no gradlew, gradle/wrapper/*, or wrapper properties (confirmed by repository file search), while README instructs gradle assembleDebug (aalam-repo/README.md:11-19). A clean CI/dev image without a system Gradle cannot build; Gradle 8.5/AGP 8.5.2 also requires a compatible JDK (typically 17), which is undocumented. Add/commit Gradle wrapper, pin distribution, document JDK 17, and invoke ./gradlew.
BLOCKER CI: no .github/workflows files (file search returned none), so there is no automated compile, unit-test, lint, manifest, or dependency validation. Add CI running wrapper-based :app:assembleDebug, :app:testDebugUnitTest, lint, and ideally connected/instrumented tests with JDK 17.
HIGH build verification gap: Gradle could not be executed in this environment (gradle: command not found), so dependency resolution/compile remains unverified. aalam-repo/app/build.gradle.kts:9,35-41 requires compile SDK 35 and Java/Kotlin 17; document/install these prerequisites and validate AGP 8.5.2 + Kotlin 2.0.21 + Compose compiler plugin together.
HIGH release hardening: release minification is explicitly disabled (aalam-repo/app/build.gradle.kts:24-30; aalam-repo/app/proguard-rules.pro:1). This increases APK size and exposes implementation; run R8 for release, add keep rules only from shrinker failures, and test minified APK (especially ML Kit/OpenCV/accessibility reflection).
HIGH Android privacy/data exposure: android:allowBackup="true" (aalam-repo/app/src/main/AndroidManifest.xml:7-14) permits backup/restore. Only DataStore/shared preferences appear intended, but onboarding/settings can still be exported and future sensitive data could leak. Set an explicit backup policy (or disable backup), add Android 12+ data-extraction-rules, and test restore behavior. backup_rules.xml:1-3 includes all shared preferences only with clientSideEncryption; it does not document the policy or cover newer extraction rules.
HIGH sensitive capability disclosure/abuse risk: manifest grants overlay, media projection FGS, notifications (AndroidManifest.xml:2-5) and exports the accessibility service (:35-45, required for system binding). This is powerful screen/gesture access. Minimize accessibility event types/flags, explain collection/use/retention and a stop/revoke flow in UI/privacy docs, and add abuse/regression tests ensuring no click absent stable validated target. README claims safeguards (README.md:31-33) but provides no privacy policy or data-flow statement.
MEDIUM Android compatibility: targetSdk=34 with compileSdk=35 (app/build.gradle.kts:9-14) is increasingly behind Play/Android platform requirements; upgrade target to current supported SDK after testing. Android 14+ mediaProjection/foreground-service behavior should be tested on API 34/35. Code calls getMediaProjection and starts mediaProjection FGS (AutopilotService.kt:110-140,300-310), but has no automated API-level integration coverage.
MEDIUM service lifecycle robustness: AutopilotService returns START_NOT_STICKY (AutopilotService.kt:90-98), so process death silently stops monitoring; README does not state this. Either explicitly document non-resumption or persist state and safely recover. Fatal startup errors call startForeground before validation and then stop (:108-119,249-265); add lifecycle tests for missing/invalid projection and repeated starts.
MEDIUM repeated-action safety bug risk: after a successful click, the stable candidate is not cleared (AutopilotService.kt:170-190), so subsequent stable frames can trigger repeated clicks on the same target. Reset candidate/count after execution and add a regression test proving one action per candidate/appearance.
MEDIUM test coverage is narrow: only pure matcher/detector tests exist (app/src/test/.../PriceMatcherTest.kt:9-31, PriceParserTest.kt:9-29, TargetDetectorTest.kt:11-26). No tests cover ViewModel validation/persistence, service lifecycle, projection cancellation, permission/onboarding flows, accessibility dispatch, overlay, frame recycling/concurrency, notification/FGS requirements, backup, or release/R8 behavior. Add unit, Robolectric/instrumented, and UI tests. PriceParserTest.kt:3 and TargetDetectorTest.kt:3 use android.graphics.Rect in local JVM tests; depending on implementation this can hit Android-stub runtime failures—run tests and use Robolectric or JVM-safe geometry types if needed.
MEDIUM notification/resource issue: AutopilotService.kt:314-318 uses the 108dp multicolor vector ic_autopilot.xml:1-15 as a notification small icon. Android status-bar icons should be white/alpha-only; some devices render this badly or reject it. Add a dedicated monochrome notification icon and adaptive launcher resources (currently only drawable vector is present; manifest :10-12).
LOW documentation misleading/incomplete: README says “Open repository root” (README.md:5-9) but the Android Gradle root is aalam-repo, not workspace root (workspace is a pnpm monorepo). It says “run gradle” without wrapper/JDK prerequisites (:11-19), and describes runtime permission flows but omits notification permission/API 33 behavior, backup/privacy, stop-on-process-death, and target SDK support. Correct paths/commands and add troubleshooting.
LOW gitignore: nested Android ignore is good (aalam-repo/.gitignore:1-9), but root .gitignore:3-7 omits generic build/, .kotlin/, .externalNativeBuild/, and Android APK/AAB outputs. Current nested app build is covered, but future modules/root Gradle outputs can be accidentally committed. Add repository-wide Gradle/Android patterns while preserving intentional artifacts.
LOW resource/localization/accessibility: user-facing permission dialog strings are hardcoded in MainActivity.kt:230-251, while notification/accessibility strings are in res/values/strings.xml:2-9. Move all UI text to resources, add translations/content descriptions, and test TalkBack. themes.xml:1 declares tools namespace unused (:1).
LOW security hygiene: no secrets were found in scanned Android source/config, and service intents are explicit/non-exported (AndroidManifest.xml:26-33), which is good. Still add lint, dependency vulnerability/SBOM checks, signed-release configuration outside source control, and verify exported-component and backup lint checks in CI.